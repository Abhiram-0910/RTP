# ─────────────────────────────────────────────────────────────────────────────
# MIRAI — Python dependencies
# Architecture: FastAPI + async SQLAlchemy + pgvector + sentence-transformers
#               + Gemini 1.5 Flash + LangChain + cachetools TTLCache
# ─────────────────────────────────────────────────────────────────────────────

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.110.0
uvicorn[standard]==0.27.1

# ── Pydantic + settings ───────────────────────────────────────────────────────
pydantic>=2.0
pydantic-settings>=2.0

# ── Database — async SQLAlchemy + asyncpg + pgvector ─────────────────────────
sqlalchemy[asyncio]==2.0.28
asyncpg==0.29.0
pgvector==0.2.5

# ── Async HTTP (TMDB data collector) ─────────────────────────────────────────
aiohttp==3.9.5
tenacity==8.2.3

# ── AI — embeddings ───────────────────────────────────────────────────────────
sentence-transformers==2.2.2
numpy==1.26.4
scikit-learn==1.4.1.post1

# ── AI — vector search fallback ───────────────────────────────────────────────
faiss-cpu==1.7.4

# ── AI — LLM + RAG ───────────────────────────────────────────────────────────
langchain==0.1.16
langchain-community==0.0.38
langchain-google-genai==1.0.3
google-generativeai>=0.5.0

# ── Language detection ────────────────────────────────────────────────────────
langdetect==1.0.9

# ── In-memory cache fallback ──────────────────────────────────────────────────
cachetools==5.3.3

# ── Environment / config ──────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── Translation & NLP ─────────────────────────────────────────────────────────
deep-translator==1.11.4
textblob==0.17.1

# ── API Extras ───────────────────────────────────────────────────────────────
slowapi==0.1.9
psycopg2-binary==2.9.9

# ── Infrastructure ───────────────────────────────────────────────────────────
redis==5.0.1
celery==5.3.4